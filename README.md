📌 RAG Chatbot – Laravel + OpenRouter + Vector Search

A simple Retrieval-Augmented Generation (RAG) chatbot built using Laravel, OpenRouter LLMs, PDF/Text ingestion, Chunking, Embeddings, and Vector Search.

Users can upload documents (PDF/TXT) → backend extracts text → chunks → embeddings store → chatbot retrieves similar chunks → LLM generates context-aware answers.

🚀 Features 
✅ Document Upload (PDF/Text)
✅ Automatic Chunking
✅ Embeddings using OpenRouter (free LLMs)
✅ Vector Search (Cosine similarity or DB query based)
✅ RAG Chat Completion
✅ Background Queue for Document Processing
